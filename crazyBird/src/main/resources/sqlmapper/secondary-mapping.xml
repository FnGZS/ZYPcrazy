<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.crazyBird.dao.secondary.SecondaryDao">
<resultMap id="SecondaryGoodsDTOMap" type="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsDTO" >
    <result column="id" property="id" jdbcType="BIGINT" />
    <result column="user_id" property="userId" jdbcType="BIGINT" />
    <result column="views" property="views" jdbcType="INTEGER" />
    <result column="goods_num" property="goodsNum" jdbcType="INTEGER" />
    <result column="user_name" property="userName" jdbcType="VARCHAR" />
    <result column="headimgurl" property="headImgUrl" jdbcType="VARCHAR" />
    <result column="goods_title" property="goodsTitle" jdbcType="VARCHAR" />
    <result column="goods_content" property="goodsContent" jdbcType="VARCHAR" />
    <result column="goods_img" property="goodsImag" jdbcType="VARCHAR" />
    <result column="postion" property="postion" jdbcType="VARCHAR" />
    <result column="goods_type" property="goodsType" jdbcType="VARCHAR" />
     <result column="status" property="status" jdbcType="INTEGER" />
    <result column="goods_way" property="goodsWay" jdbcType="VARCHAR" />
    <result column="trading_way" property="tradingWay" jdbcType="VARCHAR" />
    <result column="goods_price" property="price" jdbcType="DECIMAL" />
    <result column="goods_old_price" property="oldPrice" jdbcType="DECIMAL" />
    <result column="gmt_created" property="gmtCreated" jdbcType="TIMESTAMP" />
</resultMap>
<resultMap id="SecondaryGoodsCommentsDTOMap" type="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsCommentsDTO" >
    <result column="id" property="id" jdbcType="BIGINT" />
    <result column="school_num" property="schoolNum" jdbcType="BIGINT" />
    <result column="content" property="content" jdbcType="VARCHAR" />

    <result column="headimgurl" property="headImgUrl" jdbcType="VARCHAR" />
	<result column="reply_name" property="replyName" jdbcType="VARCHAR" />
	<result column="replyed_name" property="replyedName" jdbcType="VARCHAR" />
    <result column="gmt_created" property="gmtCreated" jdbcType="TIMESTAMP" />
    
</resultMap>
<resultMap id="UserAddressDOMap" type="com.crazyBird.dao.secondary.dataobject.SecondaryUserAddressDO" >
	 <result column="id" property="id" jdbcType="BIGINT" />
    <result column="user_id" property="userId" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />

    <result column="telephone" property="telephone" jdbcType="VARCHAR" />
	<result column="address" property="address" jdbcType="VARCHAR" />
</resultMap>
  <sql id="Base_Column_List" >
   id,field
  </sql>
  <select id="getSecondaryGoodsCount" resultType="java.lang.Integer" parameterType="java.lang.Integer">
  	select 
	count(1)
  	from secondary_goods 
	<if test="goodsType != null and goodsType != 0">
  			where 	 goods_type=#{goodsType,jdbcType=INTEGER}
  	</if>
  	
  </select>
   <select id="getSecondaryGoodsList" resultMap="SecondaryGoodsDTOMap" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsPO">
	select sg.id ,u.user_name,sg.`status`,u.headimgurl,sg.user_id,sg.goods_num,sg.goods_title,sg.goods_content,sg.goods_img,sg.goods_price,sg.goods_old_price,sg.postion,sg.views,sw.field as goods_way ,stw.field as trading_way ,st.field as goods_type, sg.gmt_created 
	from secondary_goods as sg 
	LEFT JOIN secondary_way as sw on sw.id=sg.goods_way 
	LEFT JOIN secondary_type as st on st.id=sg.goods_type
	LEFT JOIN secondary_trading_way as stw on stw.id = sg.trading_way LEFT JOIN `user` as u ON u.school_num=sg.user_id where sg.`status`=1 
	<if test="goodsType != null and goodsType != 0">
  		and goods_type=#{goodsType,jdbcType=INTEGER}
	</if>
	order by gmt_created desc
	limit #{startPos,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
  	</select>
	<select id="getUserAddress" parameterType="java.lang.Long" resultMap="UserAddressDOMap">
		select * from secondary_address where user_id=#{userId,jdbcType=BIGINT}
	</select>
  
   <select id="searchSecondaryGoodsCount" resultType="java.lang.Integer" parameterType="java.lang.String">
  	select 
	count(1)
  	from secondary_goods where goods_title LIKE CONCAT(CONCAT('%',#{keyWord,jdbcType=VARCHAR},'%'))
  </select>
  <update id = "updateUserAddress" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryUserAddressDO">
  update secondary_address set 
  <if test="name !=null">
  name=#{name,jdbcType=VARCHAR},
  </if>
  <if test="userId !=null">
  user_id=#{userId,jdbcType=BIGINT},
  </if>
  <if test="telephone !=null">
  telephone=#{telephone,jdbcType=VARCHAR},
  </if>
  <if test="address !=null">
  	address=#{address,jdbcType=VARCHAR}
  </if>
  where id=#{id,jdbcType=BIGINT}
  </update>
  
  <insert id = "addUserAddress" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryUserAddressDO">
 insert into secondary_address (
  id,
  user_id,
  telephone,
  address,
  name
 )
 values
 (
  #{id,jdbcType=BIGINT},
  #{userId,jdbcType=BIGINT},
  #{telephone,jdbcType=VARCHAR},
  #{address,jdbcType=VARCHAR},
  #{name,jdbcType=VARCHAR}
 )
  </insert>
   <select id="searchSecondaryGoods" resultMap="SecondaryGoodsDTOMap" parameterType="com.crazyBird.dao.secondary.dataobject.SearchSecondaryGoodsPO">
	select sg.id ,u.user_name,u.headimgurl,sg.user_id,sg.goods_num,sg.goods_title,sg.goods_content,sg.goods_img,sg.goods_price,sg.goods_old_price,sg.postion,sg.views,sw.field as goods_way ,stw.field as trading_way ,st.field as goods_type, sg.gmt_created 
	from secondary_goods as sg 
	LEFT JOIN secondary_way as sw on sw.id=sg.goods_way 
	LEFT JOIN secondary_type as st on st.id=sg.goods_type
	LEFT JOIN secondary_trading_way as stw on stw.id = sg.trading_way LEFT JOIN `user` as u ON u.school_num=sg.user_id where sg.`status`=1 

  	and goods_title LIKE CONCAT(CONCAT('%',#{keyWord,jdbcType=VARCHAR},'%'))

	order by gmt_created desc
	limit #{startPos,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
  </select>
  
  <select id="getSecondaryGoodsCountByUser" resultType="java.lang.Integer" parameterType="java.lang.Long">
    select 
	count(1)
  	from secondary_goods where user_id=#{userId,jdbcType=BIGINT} 
  </select>
	<select id="getSecondaryGoodsByUser" resultMap="SecondaryGoodsDTOMap" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsByUserPO">
	select sg.id ,u.user_name,u.headimgurl,sg.user_id,sg.goods_num,sg.goods_title,sg.goods_content,sg.goods_img,sg.goods_price,sg.goods_old_price,sg.postion,sg.views,sw.field as goods_way ,stw.field as trading_way ,st.field as goods_type, sg.gmt_created 
	from secondary_goods as sg 
	LEFT JOIN secondary_way as sw on sw.id=sg.goods_way 
	LEFT JOIN secondary_type as st on st.id=sg.goods_type
	LEFT JOIN secondary_trading_way as stw on stw.id = sg.trading_way LEFT JOIN `user` as u ON u.school_num=sg.user_id where sg.`status`=1 

  	and user_id=#{id,jdbcType=BIGINT} 

	order by gmt_created desc
	limit #{startPos,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
  </select>
  <select id="getSecondaryGoods" resultMap="SecondaryGoodsDTOMap" parameterType="java.lang.Long">
	select sg.id ,u.user_name,u.headimgurl,sg.user_id,sg.goods_num,sg.goods_title,sg.goods_content,sg.goods_img,sg.goods_price,sg.goods_old_price,sg.postion,sg.views,sw.field as goods_way ,stw.field as trading_way ,st.field as goods_type, sg.gmt_created 
	from secondary_goods as sg 
	LEFT JOIN secondary_way as sw on sw.id=sg.goods_way 
	LEFT JOIN secondary_type as st on st.id=sg.goods_type
	LEFT JOIN secondary_trading_way as stw on stw.id = sg.trading_way LEFT JOIN `user` as u ON u.school_num=sg.user_id 
	where sg.id=#{id,jdbcType=BIGINT} 

  </select>
  <select id="getSecondaryGoodsCommentCount" parameterType="java.lang.Long" resultType="java.lang.Integer">
  select count(1) from secondary_comments where comments_id is null and goods_id=#{id,jdbcType=BIGINT}
  </select>
    <select id="getSecondaryGoodsCommentsNum" parameterType="java.lang.Long" resultType="java.lang.Integer">
  select count(1) from secondary_comments where goods_id=#{id,jdbcType=BIGINT}
  </select>
  <select id="getSecondaryGoodsComment" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsCommentsPO" resultMap="SecondaryGoodsCommentsDTOMap">
  select sc.id ,sc.content,sc.gmt_created,u.headimgurl,u.school_num,u.user_name as reply_name
  from secondary_comments as sc
  LEFT JOIN `user` as u on u.school_num=sc.reply_id
  where sc.goods_id=#{id,jdbcType=BIGINT} and sc.comments_id IS null ORDER BY gmt_created desc  limit #{startPos,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
  </select>
    <select id="getSecondaryGoodsReply" parameterType="java.lang.Long" resultMap="SecondaryGoodsCommentsDTOMap">
  	select sc.id ,sc.content,sc.gmt_created,u.headimgurl,u.school_num,u.user_name as reply_name ,us.user_name as replyed_name
	from secondary_comments as sc
	LEFT JOIN `user` as u on u.school_num=sc.reply_id
	LEFT JOIN `user` as us on us.school_num=sc.replyed_id
	where sc.comments_id=#{commentsId,jdbcType=BIGINT} order by gmt_created desc
  </select>
  <update id="createSecondaryViews" parameterType="java.lang.Long">
  	update secondary_goods set views = views+1 where id=#{id,jdbcType=BIGINT}
  </update>
  <insert id="createSecondaryGoods" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsDO">
  insert into secondary_goods 
  (
  	id,
  	user_id,
  	goods_title,
  	goods_content,
  	goods_img,
  	goods_num,
  	postion,
  	views,
  	goods_type,
  	goods_way,
  	trading_way,
  	 `status`,
  	goods_price,
  	goods_old_price,
  	gmt_created,
  	gmt_modified		
  )
  value(
  	null,
  	#{userId,jdbcType=BIGINT},
  	#{goodsTitle,jdbcType=VARCHAR},
  	#{goodsContent,jdbcType=VARCHAR},
  	#{goodsImag,jdbcType=VARCHAR},
  	null,
  	#{postion,jdbcType=VARCHAR},
  	0,
  	#{goodsType,jdbcType=INTEGER},
  	#{goodsWay,jdbcType=INTEGER},
  	#{tradingWay,jdbcType=INTEGER},
  	0,
  	#{price,jdbcType=DECIMAL},
  	#{oldPrice,jdbcType=DECIMAL},
  	now(),
  	now()
  )
  
  </insert>
  <insert id="createSecondaryGoodsComment" parameterType="com.crazyBird.dao.secondary.dataobject.SecondaryGoodsCommentDO">
  insert into secondary_comments 
  (
  	id,
  	goods_id,
  	comments_id,
  	reply_id,
  	replyed_id,
  	content,
  	gmt_created,
  	gmt_modified 	
  )
  value
  (
  #{id,jdbcType=BIGINT},
  #{goodsId,jdbcType=BIGINT},
  #{commentsId,jdbcType=BIGINT},
  #{replyId,jdbcType=BIGINT},
  #{replyedId,jdbcType=BIGINT},
  #{content,jdbcType=VARCHAR},
  NOW(),
  NOW()
  )
  
  </insert>
 
  
</mapper>